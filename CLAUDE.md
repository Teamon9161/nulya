# Nulya — 给 AI 协作者的入口

Nulya 是一个用 Zig 写的极小 agent harness：**不可变内核 + 可自演化的能力层**。
内核只暴露**一个**内置工具（`shell`），其余能力由 agent 自己制造成 extension、
由 **pin** 在 session 边界进模型工具面（usage journal 只是证据，内核不读它排序）。
内核不负责"聪明地进化"，只负责让进化 **安全、可观测、可回退、可学习**。

## 先读什么

| 文件 | 是什么 | 什么时候读 |
|---|---|---|
| 本文件 | 地图 + physics + 模块表 + 现状 | 每次开始 |
| [docs/DESIGN.md](docs/DESIGN.md) | **现状**：已实现的架构、不变量、数据格式；与代码同步 | 改代码前 |
| [docs/PLAN.md](docs/PLAN.md) | **计划**：方向修正、路线图、尚未实现的设计（演化层、driver / `/goal`、handoff、subagent、慢速回路） | 讨论方向 / 做新功能前 |
| [docs/base-tools.md](docs/base-tools.md) | shell / `emit` 的输出纪律（已实现；`edit` 那节现在归 `extensions/std`） | 改 `tools/` 或 `emit.zig` 时 |
| [docs/agents-and-review.md](docs/agents-and-review.md) | subagent 原语 + 审阅门设计——**全部未实现**，归属 PLAN | 做 subagent 时 |
| [docs/tui.md](docs/tui.md) | `tui/`（Bun + OpenTUI 前端）的设计契约 + 里程碑 + 实施日志（§11）——**T0–T33 已落地（T10 `/goal` 占位）**；内核改动只有 `session step --stream`（DESIGN §14）、`session step --gate`（DESIGN §4/§14）、`session new --parent` 的父校验 / 身份继承（DESIGN §11）与 `NULYA_EXE`（DESIGN §7.6） | 做 TUI / 改 `session step` 时 |
| `docs/history/` | 考古：拆分前的完整 DESIGN、v0.1 开发历史 | 不用读 |

**铁律：DESIGN.md 只写已落地的东西；PLAN.md 写将来。**
改了内核语义，同一个 commit 更新 DESIGN.md；把计划写进 DESIGN.md 是 bug（AI 会把它当现状）。

## 八条 physics（所有代码都在其上运行，任何 extension / driver 都改不了）

1. **Ledger 只能 append。** `ledger.zig` 唯一写口是 `append`；纠正 = 再 append 一条。
2. **Session composition 在 `init` 冻结。** tools / skills / system prompts / extension 版本整场不变；中途 activate 新版本只影响 CLI 路径与下一场 session。
3. **model-visible 状态只经 append 改变。** extension 只 *propose*，kernel *append*，PromptIR *project*；没有任何东西能 rewrite system prompt / messages。
4. **换 composition = 换 session。** 不存在 `setTools` / `setSystemPrompt` 之类的动词。
5. **Extension version 内容寻址、不可变。** `activate` = 原子改 `current` 指针（回滚就是 activate 旧版本，没有第二个动词）；旧版本永远保留。
6. **Authority 不隐式增长。** `extension ⊆ shell ⊆ session`；secret 形状的 host env 永不下传给子进程。
7. **Cancellation 只有一个 kernel 语义。** 在 step 边界消化，ledger 永远处于合法状态（assistant-with-calls 后必有一条匹配的 tool_results）。
8. **智能不进内核。** "该不该造工具 / 什么值得留下 / 何时该继续" 是 agent 或可替换 policy 的事，kernel 只存 facts、给 primitives。

## 现状一句话（2026-08）

- **已跑通**（`tests/e2e.zig` 真实二进制全环）：durable ledger 文件（header 冻结 composition + `seq` JSONL）→ PromptIR → 一次 step（批量 tool call、**一条** tool_results 回传、串行执行、可取消）→ shell → `nulya ext init|build|activate|run` → usage journal → 下一场 session 边界**按 pin**（`registry.pinned_native_tools` 或 `session new --pin`）进 native 工具面并按冻结版本执行（usage 自己绝不改 `tools[]`）；`createDurable/openDurable` 让 session 落盘、任意进程 resume 出 turn 级相等的 PromptIR、跨进程 capability-note 经 inbox 在 step 边界排干。**durable correctness（M2.1）**：单写者由 `<id>.lock` 排他 advisory 锁强制（第二写者 `SessionBusy`，读者不挡）；inbox 事件带 `origin` 投递去重列 → 应用 exactly-once（崩溃/重投不重复）；模型身份创建时**单处** credential-aware 解析（`resolveDescriptor`）并冻结进 header `model_identity`，运行 handle 只从该 descriptor 建（跑的 == 冻结的），resume 只重解 credential、无静默 fallback（缺密钥即 `MissingCredential`；durable credential 只经 `api_key_env`，inline `api_key` 不参与）。
- **也跑通**：`nulya session new|append|step|events|cancel`（只有 `step` 写 session 文件：`append` 投 inbox、`cancel` 写标记、`events` 只读 tail；`step --max-steps` 由 kernel 夹到 `session.max_steps_ceiling`；cancel 标记由 kernel 在 step 边界消费，mid-run 也停得下来；`new --profile P [--model ID]` 选 profile 与它服务的 model id，`step --effort E` 是每步的 generation option）；`nulya config show [--json]`（有效 profiles + credential 可用性 + `[[models]]` 目录 + 合并后的 `[registry]`（`max_tools` / `pinned_native_tools`）的投影，供选择器与 agent 自查，无 secret——不投影 pin 的代价实测过：模型只能去 `cat` 三层 config，把 user 层 api_key 打进了转录；**codex profile 不写 `models`**——ChatGPT 订阅的模型清单与每个模型的参数（订阅的有效窗口、多出的 `xhigh`/`max`/`ultra` 档、各自的默认）读自 Codex CLI 的 `models_cache.json`，投影成 per-profile 的 `catalog`（与 `models[]` 逐位对应，其余 profile 恒 `null`——同一个 id 经订阅与经公开 API 是两套数字，id-keyed 的 `[[models]]` 说不了它），写了 `models` 的层照旧胜出，读不到文件退回 `model`；`nulya config refresh` 用同一套 OAuth 打一次 `/codex/models` 只替换该文件的 `models` 列（唯一联网的一步，失败照打投影 + exit 1）；`Config.defaultEffort` 在 codex profile 上也就此止于 `p.effort`，DESIGN §9.5）；`nulya demo`（原来的 bare `nulya`）现走 durable session 路径；**脚本 extension**（`runtime.entry` 前缀区分 `bin/` 编译 vs `src/` 脚本 + `interpreter?`；脚本不编译、data/script version 不含 compiler+target（`manifest.ImplementationKind`，纯 skill/prompt 建时免 zig）；**`ext init` 缺省就是脚本**（`--zig` 才编译）、`ext run --arg k=v`；2026-08-23 起 `runtime.wire: "plain"` 是**唯一**要写的 wire——stdin 是参数 JSON、`NULYA_TOOL` / `NULYA_ARG_<k>` 进 env、stdout 原文就是结果、退出码是 ok，`entry` / `interpreter` 可按 OS 写成对象，一个版本跨平台；见下面 ext-review 与 ext-review-3 那两条）；**`nulya src`（M3）**：build.zig 把整个 `src/**` `@embedFile` 进二进制，`nulya src [path]` 打印真实源码（AI 读内核零 API 漂移），默认剥 top-level `test` 块、`--tests` 原样；`ext api` 协议 topic 成为 `nulya src extension/protocol.zig` 的特例。
- **也跑通（M4）**：四个 provider——`openai`（chat/completions）、`anthropic`（Messages，两个 `cache_control` breakpoint：冻结 system 尾 + 最后一条 message 的最后一块，后者随 append 前移）、`codex`（ChatGPT 订阅的 responses 端点，OAuth 走 `~/.codex/auth.json`、401 自动 refresh 回写，prompt cache key 由 **durable session id** 派生 → 跨 `session step` 进程同域）、`scripted`；三个真实 provider 的 POST + SSE + `reasoning` item 序列化收进共享的 `providers/wire.zig`。`zig build integration`（唯一联网、无 `NULYA_INTEGRATION_PROFILE` 即 skip）在 deepseek 的 openai 口 / anthropic 口 / codex 上实测：连续四步 `cache_read` 单调不减且 ≥ 上一步 input 的 90%。
- **也跑通（TUI，仓库顶层 `tui/`，不在内核范围）**：内核侧为它加的只有 `session step --stream`（`loop.StepContext.observer` 纯观测钩子 + 行协议，DESIGN §14；不带 `--stream` 一字未变）和下一条的 fork 原语。前端是 Bun + OpenTUI 的 driver 客户端——transcript 卡片与折叠、`tui.toml` 设定与 keymap 覆盖、`/sessions` `/ext` `/usage` `/settings` `/help` `/compact [focus]`、`/model` 选择器（读 `nulya config show --json`；一行一个能跑的 (provider, model)、←→ effort、Enter 定下一场跑什么；上次选择记在 `tui-state.json`）、**开屏是 draft tab、第一条消息才 `session new`**（tui.md T22：pin / 激活 / 模型都在那一刻现读，没有标题行，模型 + effort + `tools 1+N` 在输入框下面那一行；`/ext` 列出只有源码的 id、Enter 是一个 activate+pin 的开关）与 `/provider`（F6：一行一个 profile 的状态、`s` 贴 key、`a` 加 compatible endpoint、Enter 在能跑的 provider 上跳回 `/model` 落在它的第一个模型；一个 provider 都跑不了时开屏即 `/provider`，否则开屏即 `/model`——tui.md T21）、`/effort`、sub-session tab、observer 模式（`<id>.lock` 探针 + 内核 `SessionBusy` 双信号）、`bun build --compile` 单文件；**M5 的四个面也已消费（T8，内核零改动）**：`/sessions` 改读 `session list --json`（含 verdict / usage / composition）、`/outcome <verdict> [note]`、`/evolve` 与 `/with <id>[@<v>]`（`ext build` → `session new --with`，**不 activate**；T24 把 `/mode` 这个名字让给了权限档，T36 把 `/as` 改名 `/with`——一个概念一个词，旧名仍认）、token 计数以 ledger `assistant.usage` 为准（重开一场也看得见成本）、`/ext` 认多 store root 与 `shadowed`。**T11（启动即安装，内核零改动）**：`tui.toml` `[extensions] sync_on_start/auto_activate`；user store 在后台 `ext sync`（状态栏 `syncing extensions… 2/3` + 一行汇总），project store 在开屏前问一句（未信任且有 draft → `t` trust+build+activate / `s` 只 build / `n` 不动，只问一次，记在 `tui-state.json`；判据全借内核——draft 列表来自 `ext sync --dry-run`，信任来自读 `trusted-stores.jsonl`）；`/ext` 每个 id 多一列 draft 状态、`a` 在 id 列表上指 draft 的版本、`p` = `ext prune <id>`。**T24（权限）**：TUI 永远以 `--gate --stream` spawn step，`/mode ask|unsafe` 两档（T31 改名前叫 `auto`）+ `tui.toml [approvals]` 三张表（deny → 本场 always → ask → allow → manifest `readonly` → mode）决定问不问，问就是**输入框上面一个对话框**（T28；一行一个答案、`↑↓`/数字/悬停移光标、`Enter`/单击作答，五个答案按影响范围从窄到宽排到 `mode unsafe` 与 deny，底下常驻 note 字段——`Tab` 或直接打字进入、**note 跟着被选中的答案走**：deny 用内核的 `deny <note>`，allow 走 `session append`（`approvalnote.ts` sentinel，落在同批 tool_results 后面）；tool 卡上只留一行标记说是哪个 call），对话框在时拿键盘（`Ctrl+C` 除外），切 mode 会当场重裁正等着的请求；模型自己写的 handoff 文件（`.nulya/handoffs/<id>-*.md`）每步之后看一眼，`ask` 弹面板 `Enter` 跟 / `Esc` 收、`unsafe` 直接跟（跟 = `/compact` 的 `brief_file` 分支），每场 session 默认带 `--with handoff@<v> --pin ext:handoff/handoff`。**T29（后台任务，内核零改动）**：唯一的新 policy 是 `driver 角色 + idle + <id>.inbox/ 非空 → 再 step`（搭 `<id>.lock` 探针那个 idle 定时器，**inbox 为空绝不裸 step**，observer 不踢，`ask` 也踢），其余全是把内核已经答出来的东西画出来——`nulya task list --session <id> --json` 喂状态栏 `⠋ N background`（idle 时也显示）与 `/tasks`（F7：`Enter` 看 log 最后 64 KB、`k` kill、`K` 全杀），ShellCard 后台变体 + `TaskFinishedCard` 按**全名** `<sid>/t<N>` 连回发起它的那张卡（连接点在 ledger，重开一场照样显示），`/quit` 只提示不杀。**T31（两种「模式」都要说出自己的影响范围，内核零改动）**：① 开屏那趟后台 sync **永不 activate 一个 contribute 了 system prompt 的包**（`extensions.autoActivatable`——那是「模式」，activate = 它的 prompt 进这台机器上的每一场 session，DESIGN §5.3/§7.8；原来只靠 `arrived` 挡，于是 `evolution` 被悄悄激活、每个模型都以为自己是 slow loop，`docs/BUGS.md` 第一条），`/ext` 多一列 `mode` 并把 Enter 的后果写成一句话，开屏发现已经 active 的 mode 包就点名并指 `/ext`（**不替人关**），`/evolve` / `/help` 讲清它是「开一个新 tab 戴上它、什么都不 activate」，戴着谁在 tab 标题与状态栏 `◈ <id>` 上看得见；② 权限档 `auto` **改名 `unsafe`**（tcode 的 `Auto` 是 classifier 审核，这一档没有——它就是它的 `Unsafe`；老文件里的 `auto` 由 `approvals.normalizeMode` 一处读成 `unsafe`），切换不再解释（chip 就在那儿），裸 `/mode` 与点 chip 开一个 **picker**（输入框上面的对话框，与 T28 审批对话框同一套样子与键盘归属；一行一个 mode + 一句说明 + `✓` 当前），`/model` 顺带按 provider 分组、标题带 `◈`、与它共用同一套视觉语言。**T32（sub-agent 第一期：一个定义文件就是一组 `session new` 参数，内核零改动、`extensions/` 零改动）**：`.nulya/agents/*.md` 与 `~/.nulya/agents/*.md`（只认一层平铺、同名 workspace 胜出并点名输的那个、坏定义 warn-and-skip——只有"没 front matter"与"没正文"会被跳过），front matter 每个字段都是 `session new` 的一个参数（`name` / `description` / `readonly` / `model` → `--profile`+`--model`（不写就继承发起它的 tab）/ `pins` → 逐个 `--pin`（形状不对的丢掉并警告——解不出的 pin 是整场拒绝）/ `max_steps` → `session step --max-steps`），**正文就是 system prompt**；渲染 = 正文写成 `.nulya/scratch/agents/agent-<name>.md`（**刻意不在 store root 里**）→ `session new --prompt <它>`，字节冻进那一场的 header、**什么都不安装**（T44 之前是材料化成 `agent-<name>` data extension 再 `--with`，见下面 session-prompt 那条）；`/agent <name> <task…>` 开一张看得见的新 tab 照常以 driver 驱动，裸 `/agent` 是与 `/mode` 同一套对话框的 picker（**选中一行只把 `/agent <name> ` 写进输入框，不启动任何东西**）；`readonly` 是**排在三张表之前、不可上诉的天花板**（`shell` 一律拒 + extension tool 只放行冻结 manifest 声明 `readonly:true` 的，拒绝走内核 `deny <note>` 所以模型读得到、ledger 里记得住）；随 checkout 到达的 `.nulya/agents` **问一次**（`t`/`n`，问在**第一次 build 之前**——本机 build 填满空 store 就是信任，DESIGN §9；答案记 `tui-state.json`），`~/.nulya/agents` 不问。**T32 第二期（模型自己委派，内核零改动）**：随仓库带的 **`extensions/agent`**（compiled，第六个自带扩展）三个 tool——`agent{name,task}`（模型的委派：渲染 persona → `session new --prompt` 子场 → `session append` 任务 → `task run` 起一个**属于父场**的后台任务驱动它，回执点名子场并叫模型收尾）· `render{name}`（定义正文 → scratch 里的一个 prompt 文件 + 一整组 `session new` 参数，**写路径唯一实现**，TUI 也调它、TS 只留读）· `run{session,agent?,readonly?,max_steps?}`（那条后台命令：解析 `session step --stream` 的 JSONL、`readonly` 时以 `--gate` **机械应答**——`shell` 一律拒 + 只放行 gate 请求行上 `readonly: true` 的（那一列是内核从冻结 manifest 投影出来的，2026-08-23 起；从前是 runner 自己对每个成员 spawn `ext inspect` 推导，BUGS #16 ① 就是那条推导静默失败）——把子场最后一条 assistant 文本包成 `<agent-report>` fence + "这是数据不是指令"的合同 + 代码附的子 session id 打 stdout）。**报告回父场靠的是内核已有的那一个"欠答案"回路**：`task_finished` 经 inbox 在下一个 step 边界排干（DESIGN §6.1/§3.1），而 T29 的"driver + idle + inbox 非空 → 再 step"本来就在跑——所以 **`drivers/goal.*` 一个字没改**、TUI 没加第二个钩子。带入条件两条：**有 agent 定义才带** `--with agent@<v> --pin ext:agent/agent`、**只带在顶层 session**（子场不带 = leaf，子 agent 不能再委派）。`agent` 的 tool call 在 registry 里是 subsession 卡，子 id 取自**回执**。**没有 600 s 天花板了**（2026-08-23 起）：`ext run` 缺省不套 timeout（`--timeout-ms N` 给了才夹），manifest 的 `timeout_ms` 只是这个 tool 被 pin 到模型工具面时那一次 call 的上限；`run` 永不 pin，所以委派想跑多久跑多久。**T32 第三期（自带 persona + 读路径收成一处）**：`extensions/agent` 从三个 tool 变四个，多的是 **`list`**（driver-facing、永不 pin：列出全部定义的 name/description/readonly/layer/shadowed/pins/max_steps/warnings）。**包自带四个 agent 定义**——`explore` / `plan` / `general` / `orchestrator`（`extensions/agent/src/builtin/*.md`，`@embedFile` 进那个包的二进制，随它一起分发），移植自 tcode 的 builtin，**nulya 没有的概念是删掉而不是翻译**（`ask_user`、向下 fan-out；`orchestrator` 整个不移植——它的意义就是 fan-out 而我们是 leaf），于是**什么都不写就有三个能用的**。**定义分三层**：workspace `.nulya/agents/*.md` > user `~/.nulya/agents/*.md` > builtin，**首个持有者胜、输的照样列出来并标 `shadowed`**（store roots 那条规则，不是 tcode 的"builtin 名字保留"——这个仓库里分层的东西一律遮蔽而非拒绝）。**读路径唯一实现 = `list`**：TS 侧 frontmatter 解析全删，picker / readonly 天花板 / 委派参数都读它（两个 parser 就是"这个 agent 是不是 readonly"的两个答案）；**唯一例外是 trust 问句**——它问在屏幕与任何 build 之前，读的是**文件名**（一次 `readdir`）不是定义。**pin 蕴含成员**（2026-08-23 起，DESIGN §5.1）：`--pin ext:<id>/<tool>` 的包不是成员时内核自己按 `current` 把它 union 进来，所以 `render` **不再**为 pins 派生 `--with`、也不再预验证；包没有 `current` 时由 `session new` 在 stderr 点名"这些是 pin 带进来的"并给出两条出路。带入条件随之简化：包自带 persona 所以"有定义才带"恒真，改成 `tui.toml` 的 `[extensions] agent`（与 `handoff` 并排）；`refreshAgents` 从 `onMount` 改成懒的（开屏编译一个包正是 T11/T23 在赶出关键路径的事）。**`extensions/std` 的 `read`/`grep`/`glob` 本来就声明了 `readonly: true`**，正是 gate 放行名单的依据，无需改动、版本不变。**T32 第四期（追问 + 谁可以委派）**：`agent` tool 变成 **`agent{name|session, task, model?}`** 两个形态二选一——`session` 往一场**已经报告过的**子场再送一轮，append-only 故子场带着它找到的一切 resume、**命中它自己的前缀缓存**（一次纠正只付一轮，重开一场要把侦察再买一遍）；四道门都在建任何东西之前：s-… 形状 · 冻结 header 必须戴着 `agent-*` prompt · **还在跑就拒绝**（判据是内核自己的 `task list --json` 投影）· frontmatter 的 `max_exchanges`（数子场 `user_text`，未声明 = 不限）。每轮仍是一个新后台任务 + 一条 `task_finished`，报告的路一条没变；`readonly` 自动仍对（runner 每次从那一场自己的 header 重算放行名单）。**并发点**：人在 TUI 子 tab 接管说话而模型同时追问，撞的是单写者语义（`SessionBusy` / 那道"还在跑"的门），行为安全。**能不能委派是被委派者定义里的 `agents: [name,…]`**：空 = leaf（默认），非空才让那一场额外带 `--with agent@<自身版本> --pin ext:agent/agent`——一个字段一处读取，不能委派的子场根本没有这个 tool；tool 侧从**本场冻结 header 的 `agent-<name>` prompt**反查白名单，不在单里就列出允许的。**深度兜底** `NULYA_AGENT_DEPTH`（runner 设给它驱动的那一步，≥3 拒绝）**防的是间接环不是攻击**——人从前端驱动子场时这个变量不在，它和白名单都是 policy。builtin 补齐第四个 **`orchestrator`**（`agents: [explore, plan, general]`、`max_exchanges: 4`、不给 pins——委派是它的全部工作）。现状与实施日志在 [docs/tui.md](docs/tui.md) §5.10 / §11，**不进 DESIGN.md**（DESIGN 是内核的现状；`extensions/agent` 本身在 DESIGN §7.8）。**T33（`/ext` tools pane 折起 driver 那一半，内核零改动）**：列表里只留有 checkbox 的行，driver tool（`pinsOnActivate` 说不该 pin 的包）折成列表下面一行 `▸ N driver tools · called with ext run, never on the model face · d shows`，`d` 或点它展开；判据 `driver && 没有 pin`（真被 pin 上的照常显示——那是能撤回的状态），展开状态不落 `tui-state.json`。理由不是"driver tool 不该显示"（extensions pane 早就在说它们的存在），是**可 pin 的一半有 `max_tools` 封顶而 driver 一半没有**：T32 一次加四个，噪音往错误方向长，而这张表的每一行本该是一个能按的开关。**T35（输入框下面那一行，内核零改动）**：mode chip 移到**行首**（每个 tool call 被裁决的立场，该在 model 之前读到）；**没有的东西不占列**——没花过钱不写 `no usage yet`、没跑过步不写 `step 0`、是写者不写 `driver`（只有 `observer · driven elsewhere` 这个例外说自己），空 `<text>` 也仍占一列所以每段用 `<Show>` 包住；**notice 盖住整行然后自己下去**（`App.noticeHold = clamp(1500 + 45×字数, 3000, 9000)`，下限正是 `Ctrl+C again to quit` 那个 offer 的有效窗口——arm 到期时同一处把它取下来；browse 提示与待答 handoff 用 `holdNotice` 声明成「屏幕正处在的状态」而非新闻）。**T38（正在发生什么自己一行，内核零改动）**：新 `ui/WorkingStatus.tsx` **只在有事发生时**画在输入框**上面**——`⠋ shell` + dim 的 ` · 12s · esc to cancel`，颜色是 `accent.assistant`（跑着的就是 assistant 那一轮），一条高斯软带扫过（`theme.shimmerColor`，移植自 tcode；**抬起每格自己的颜色**朝新 token `theme.lift`，静止时精确等于 base；`<span fg>` 在 @opentui/solid 0.5.3 不生效故一格一个 `<text>` + `Index`；`motion=false` 全关）；判据是纯函数 `activityOf`，`idle` / `canceled` / observer 的 `following` 一律 null（**不画**而不是写一个「没什么可说」的词）；钟来自 `Driver.startedAt`（`setStatus` 一处维护）。状态栏因此只剩静态描述：mode · model · tools · token · 右边的 chip。**键位提示搬成开屏一条 tip**（`Welcome.tips` + 新 glyph `✻`），状态栏的 `/help` box 删除；**`ctrl+o` / `ctrl+shift+o` 两个折叠键整个删掉**——折叠只剩点头行与 browse 模式两种**说得出自己作用在谁身上**的手势，反方向的 `/fold` 留着。 **T43（一屏该说的话，内核零改动；`extensions/agent` 一处）**：① 流式末尾那个 `▍` 光标**删掉**——它拼进的是 markdown 的 **content**，每个 delta 都重新解析一份多一个字形的文档，块边界上它被吞掉或独占一行（实测三个 delta 内 7→6→7 行地抖），而 sticky-bottom 的 scrollbox 每抖一次就是整屏重排；动效从此只在 `WorkingStatus` 那一行。② **thinking 默认 `hidden`**（`transcript.thinking` 可设回 `collapsed`），且 hidden **离开 item 列表**（`Transcript.visibleItems`）——画不出东西的 item 仍占着 `gapBefore` 给它的那行空白。③ `gapBefore` 的 `thinking → assistant = 0` 收回：屏幕上那是两张卡贴在一起。④ **run 摘要**（`render/runs.ts` + `RunCard`）：一串**跑完 + 成功 + 没有身体**的调用折成 `⋯ read ×3 · grep ×2 · shell ▸`，至少两个才算；**进不去的**是还在跑的、失败的、被取消的、回执型的（后台任务 / 子场）、`edit`、演化动作、`checklist`/`markdown`、包用代码画的卡，**以及任何声明了 `render` 的 tool**——那个声明就是包在说「我对这次调用长什么样有意见」，于是「extension 自己配置什么不被收进去」**不需要新的 manifest 字段**；人这侧的开关是 `tui.toml` 的 `run_summary`。⑤ **`SubSessionCard`**（推翻 T2 的「不做独立组件」，第二个 consumer 到了）：note 用后台 `shell` 卡**同一套读法**（`state/tasks.backgroundNote`：ledger 的报告优先，没有就用 `task list` 的实时投影），头行下面一行可点的 **`↗ open <id> in a tab`**，与 browse `Enter` 走同一个入口（`state/navigate.ts`）；顺带修好「`task_finished` 认不出 `agent` 的回执、委派卡永远不会变 done」（两种回执由 `startedTaskOf` 一处读）。**为什么不在 agent 的 tui plugin 里做**：契约 v1 的 `CardRenderer` 只回 `Line[]`、`onKey` 明说不会被调用、没有点击回调，而子场是**后台任务**在驱动、它的 `--stream` 根本不经过这个前端（`observe` 只给本前端驱动的 step 与 front tab）——要做就得给 API 加两样东西而第一个 consumer 是宿主自己。⑥ **`agent{model?}`**：`<profile>[/<model-id>]`，与定义的 `model:` **同一个 parser**（`defs.parseModelRef`），优先级 **调用 > 定义 > 继承**、取的是一对不是拼一对；`session` 形态给 `model` 是 `-32602`（那一场的身份创建时就冻死了）。
- **也跑通（compaction）**：内核只提供 **fork 原语**——`session new --parent <id>:<seq>` 校验父存在，且不点名模型时继承父 header 的冻结身份（composition 仍现解，让 fork 这个 session 边界自然吸收今天的 pin 与新版本）+ 一个 `NULYA_EXE`（子进程 env 里的本二进制绝对路径，DESIGN §7.6/§9）。压缩本身**不在内核**：何时压是 policy、压成什么是模型的判断，两者由 driver 用 `session append` + `session step` + `session new --parent` 组合出来，第一个 consumer 是随仓库带的 **`extensions/compact`**（compiled extension——driver 要解析 `session step` 打印的 JSONL；七步过程 + 两个 marker + `-32001`"什么都没动"，DESIGN §11），TUI 的 `/compact` 只是 `ext build` → `ext run compact@<v>` → 换 tab 并以 observer 跟随。摘要仍在**旧 session 内部**生成以命中最大的缓存前缀，不开子 session。DESIGN §11 / tui.md §11 T7、T9。
- **也跑通（M2c：模型主动的 handoff + 第一个 driver）**：同一条 fork 路径，换个触发者，**内核零改动**（只给 `launch.ScriptedProvider` 加了第四档离线替身）。**`extensions/handoff`**（仓库顶层、compiled）contribute 一个 `handoff{done,next_task,keep,drop?}` tool：只 propose 不 fork（`session new --parent` 全仓库仍只有 `extensions/compact` 一个调用者），缺必填节 → `-32602` 一次列全、**不落盘**，不在 session 里（无 `NULYA_SESSION`）→ 错误、**不落盘**，否则渲染成 markdown 落 `.nulya/handoffs/<session>-<n>.md`（exclusive create 取第一个空位，单调、不覆盖）并回"记录好了，别再调工具，结束本轮"——**那个文件就是提议**。**默认不在任何 composition 里**，由 driver `session new --with handoff@<v> --pin ext:handoff/handoff` 带入（`--pin` 的第一个真实 consumer）。`compact` 加 **`brief_file` 分支**：给了就跳过七步的 2–4（旧文件逐字节不变），fork 点 = 旧 ledger 当前 tail；两条路径都由**代码**在 carried 文本末尾追加父指针（含 `nulya session events <parent>`）。**`drivers/goal.sh` + `drivers/goal.ps1`**（仓库顶层 `drivers/`，各 ≤ 70 行、逐行对齐、都不解析 JSON）是第一个 driver：循环 `session step --max-steps 1 --stream`，每步**先看盘**（新 handoff 文件 → `ext run compact --arg brief_file=…` → 切到子 id），否则看协议的 `"stopped":"end_turn"` 收工；**stdout 只有控制行**（`session` / `handoff <old> -> <new>` / `done` / `evaluate:`），**stderr 是 `--stream` 行协议原样透传**——前端 spawn 它就能拿实时 token delta 并按 stdout 开/切 tab，不需要 `<id>.live` sidecar。DESIGN §11 / §13。
- **也跑通（M5，慢速回路的 substrate）**：**outcome journal**（`.nulya/session-outcomes.jsonl` + `nulya session outcome <id> <success|partial|failure> [--note] [--seq N]`——第二条 journal 而不是 ledger 事件；**没有行 = unknown ≠ failure**，最后一条作数；只写 journal、不拿 `<id>.lock`，所以正在 `step` 的场也能当场评；**记 `source`**：缺省 = 人，`NULYA_SESSION` 在环境里就是 `agent` + `by:<那场 id>`（`by == session` = 自评，是主张不是 ground truth），`--seq N` 是对某一轮的判断、不参与 `latestFor`）· **per-step usage** 落进 `assistant.usage`（`ledger.Usage`，**不投影**，与 `reasoning` 同地位；provider 没报就整条不写，老行读回 null）· **多 store root**（`store.Roots`：workspace `.nulya/extensions` → user `~/.nulya/extensions` → `extensions.paths`（**只认 trusted 层**），**首个持有者胜**，`ext list` 标 `(shadowed)` 并按冻结 manifest 标 `[tools skills prompt]`（`prompt` = 这个包 activate 后会进每一场 session 的 system blocks）；`activate --user` 在 session 里跑会往 stderr 说一句它跨出了本 workspace（照做不拦）；frozen 版本按 root 顺序找——内容寻址故等价；header 不记 root；写端 `ext init|build|activate|deactivate --user`）· **`ext build <path>` 按 manifest id 落 store**（`<root>/<id>/versions/<v>`，draft 可以待在仓库任意路径；store 内 draft 与从前字节等价）· **`session new --with <id>[@<version>]`**（可重复；composition membership **不是** native pin：skills 进 catalog、system_prompts 进 system blocks；没 `@version` 就取 `current`，没有就 exit 1；fork 不继承）· **`nulya session list [--json]`**（`.nulya/sessions/` 的只读投影：composition（含每个冻结版本贡献的 `system_prompts`）/ parent / 事件数 / usage 求和 / **episode**（沿 `parent` 链算出的 `root` + 同 root 求和的 `episode_usage`，只在投影里连接，journal 不动）/ 最新 verdict（含 `source`/`by`））+ `session new` 开始写 header `created` · **`extensions/evolution/`**（仓库顶层、与 `src/` `tui/` 同级的 data extension：identity system prompt + skill；**不 activate**，用 `nulya ext build extensions/evolution` 拿到 version 再 `session new --with evolution@<v>`）· **M5.1 收尾**：两条 journal 的 append 持 `<journal>.lock`、读端忽略残尾；`<root>/<id>/.lock` 包住 build / activate / deactivate；`ext activate` 落到该 id **生效中**的 root（版本不在那就失败指路，只在真生效时投 capability_note）；`ext run <id>@<version>` 点名 built 版本（`--with` 带进来的 runtime tool 的调用形式）；**`max_tokens` 进 loop lifecycle**：截断的 turn 记 calls **原样**（连半截 JSON 一起——ledger 存事实）、不执行、marker 批次告诉模型，可回放由**投影**保证（`prompt.project` 把这一 turn 里不是完整 JSON 值的参数换成 `{}`），`run` 连续两次截断即停，`--stream` 报 `stopped: max_tokens`。
- **也跑通（workspace store 的 trust gate，DESIGN §9）**：`.nulya/extensions` 在 checkout 里又是第一优先 root，所以一个**随 clone 到达**的 store 要被信任一次才进 composition。判据是**出生地不是内容 hash**（hash 每轮自建自 activate 都会变，那样的门会卡死自演化）：本机 `ext build`（非 `--user`、落 workspace root、build 前该 store 为空）成功后**自动**记一条信任；已持有内容却无记录 = 随 checkout 到达 → `session new` / `session step` **硬拒**（stderr 点名 store 路径 + 列出 `id@version` 及 `[tools skills prompt]` + 指路 `nulya ext trust`，exit 1），不静默剔除该 root。`nulya ext trust` 是显式信任（先打印要信任的东西再记录；空 store = `nothing to trust`，重复 = `already trusted`，无 `untrust`——撤销 = 手删行）。"持有"= 某个 `<id>/` 有 `current` 或有 built 版本（draft / 失败 build 的空壳都不算），判据只有一处实现（`launch.occupiedWorkspaceStore`）。记录在 **user 层** `<NULYA_HOME | ~/.nulya>/trusted-stores.jsonl`（`journals/trust.zig`，第三条 journal——不是证据而是一次授权；project 层记不算数，否则 checkout 自己签名）。**门在壳层**（`launch.ensureWorkspaceStoreTrusted` + `cli/session.zig`），`composition.zig` / `session.zig` 与库路径 `AgentSession.init` 都不知道它存在；只读投影（`ext list|inspect`、`skill list|load`）与 `ext run` **不过门**——它们正是决定要不要信任所需的工具。
- **也跑通（review 修补）**：assistant 事件记的是 provider 的 **`stop_reason`**（`ledger.StopReason`，取代 `truncated: bool`——落盘只写 shape 说不出来的 `max_tokens`/`other`，`lastStopReason()` 因此是一次 ledger 读而不是活在进程里的字段）· header 记 **`nulya{version, kernel_hash}`**（build 版本串 + kernel prompt 与 builtin 定义的 hash；**纯 provenance**，resume 对不上只在 stderr 警告一行照跑，空 stamp = 老 session = 不警告）· **shell 超时**（`{command, cwd?, timeout_ms?}`，默认 120s / 上限 600s，`tool.Timeouts` 一张表也管 extension 的 30s；超时与取消都杀**整棵进程树**（`environment.Tree`）并把已捕获的输出连同 `[timed out after …]` 一起返回，`ok=false`）。
- **也跑通（guide：harness 的自描述入口）**：自描述面（`nulya src` / `ext api` / `config show` / `skill list|load`）早就有，缺的是**入口**，现在补了三层、**内核语义零改动**（只动 `composition.zig` 的那一个字符串常量与 `cli/` 的文本）。① **kernel prompt 加一句事实**（57 词，DESIGN §7.5）：`NULYA_EXE` 给出本二进制路径（安装后叫 `nulya`）、`nulya help` 列能力、`nulya src` 打印本 harness 源码，以及 extension / skill / system prompt / session driver 都是模型在任务需要时可以写的东西——**没有一个字是"你应该进化"**（该不该造是判断，判断住在 kernel 之上）；代价是 `kernel_hash` 变一次（老 session resume 警告一行照跑）。② **`nulya help`**（也认 `--help` / `-h`，43 行一屏）：`cli/common.zig` 把 usage 拆成按动词族的常量，`help` 拼一屏、bare `nulya ext|session|skill|config|toolchain` 各印自己那块（`cli/session.zig` 里那份重复的 session usage 已删），与 DESIGN §14 命令表逐动词对齐；未知命令 → `unknown command '<x>'; run \`nulya help\``。`ext api permissions` 重写成今天的 authority、`examples` 覆盖 script init → build → run → activate → `--pin` / `--with` → `--user` → `trust` → `outcome`。③ **`extensions/guide/`**（仓库顶层 data extension，与 `evolution` 同层）：只 contribute 一个 skill（**不**贡献 system prompt——那是每步都付的前缀；skill 是渐进披露），187 行英文参考手册，只指路 + 最短配方，真相留在 `nulya help` / `ext api` / `src` / `config show` 里。**不 activate**，装是用户的决定：`nulya ext build extensions/guide --user` → `nulya ext activate --user guide <v>`。model-facing 文本（kernel prompt / usage / `ext api` 两个 topic / SKILL.md）**零文档引用**，e2e 断言。
- **也跑通（`extensions/std`：tcode 体验的六个 tool，一个 extension，内核一处改动）**：`read` / `write` / `append` / `edit` / `grep` / `glob` 作为一个随仓库带的 **compiled** extension（DESIGN §7.8）——行为逐条移植自 tcode（零猜测的错误文案 + 自分页 + `read` 无行号并把 <120 行的小读放大 + freshness 去重 / `write` 覆盖门 / `append` 门 + `grep` smart-case / per-file 30 上限 / 按匹配分页 / gitignore + `glob` 按 mtime 200 上限），砍掉 vision / redaction / 后台任务；**默认不在任何 composition 里**，用户 `ext build extensions/std --user` → `activate --user` → user config `[registry] pinned_native_tools = ["ext:std/read", …]`（1 + 6 = 7 ≤ `max_tools` 20）。**内核唯一改动**：extension 的 `result` 是**字符串**时按原文进 `emit`（DESIGN §7.3；plain wire 上就是 stdout 原样），否则读文件的 tool 每次都让模型读转义 JSON。跨调用状态只有一个磁盘制品 `.nulya/scratch/<sid>/std-freshness.jsonl`（`NULYA_SESSION` 取 id；不在 session 里就无门无去重；六个 tool 共用它，所以 read → edit → write 不再被拦——那条 D4 代价随 `edit` 搬进来一起消失了）。regex = vendored mvzr（Zig 0.16、单文件、MIT）、gitignore / glob 匹配移植自 zeegrep 两个模块、walker 自写单线程 10 s deadline——**不依赖 rg**。extension 自己的纯逻辑模块有单测并挂在 `zig build test` 里（build.zig 第二个 `addTest`）；e2e 在 `tests/e2e/std*.zig`。契约与进度：`docs/goals/std.md`。
- **也跑通（sync：把源码放进 store 目录就能用）**：布局本来就是"draft 住 `<root>/<id>/`、冻结版本在旁边 `versions/`"，缺的只是壳层动词，于是补了三件事、**core 一字未动**（`composition` / `session` / `ledger` / `loop` / `prompt` / `store` 的语义全不变）。① **`ext build` 跨 root 复用**：编译前按 root 顺序在别的 root 找同一份 snapshot（seal 的 `package_digest` + `target` +（能问出编译器时）`compiler`），找到就整树复制 + 再验一次 integrity，打 `(built, copied from <root>, in <dest>)`——内容寻址的直接推论；匹配键是 seal 三元组而非算好的 `v`，所以**没有 zig 时也能装上 user store 里已有的 compiled 版本**（`compilerIdentity` 不再提前失败，真要编译时才报），DESIGN §7.4。② **`nulya ext sync [--user] [--activate] [--dry-run]`**：把该 root 下每个 draft（`<root>/<id>/extension.json`，只认一层）走一遍同一条 build 路；**一个失败不中断其它**，每 id 一行、结尾一行汇总、有失败 exit 1；`--activate` 只指向"这趟新拿进来的"和"根本没有 `current` 的"，**`current` 指着别处的一律不动**（rollback 活得过下一次 sync）；`--dry-run` 走 `build_ext` 的 `Mode.plan`（同一份计算，写之前停手），所以它与真跑不可能说两样话。③ **`nulya ext prune [--user] [<id>] [--dry-run]`**：删非 `current` 的版本目录（持 `<id>/.lock`），**无 `current` 的 id 一个都不删**，并诚实打印代价（冻在被删版本上的 session 无法 resume；同源码重 build 得同 id）。④ **`nulya ext seed [--user] [<id>…] [--force] [--dry-run]`**（`src/cli/ext_seed.zig`）：build.zig 把仓库自己的 `extensions/**` 按 `src_embed` 同一先例 `@embedFile` 进二进制（`src/bundled.zig` 投影），seed 把这些自带 draft 写进 store root——**分发就是二进制本身**，不在 nulya checkout 里也拿得到八个自带扩展；只写源码（build 归 sync、门一道不少），`--dry-run` 连 root 目录都不建。**它也是自带扩展的更新通道**：每写一个 draft 就在 `<root>/<id>/.seed` 记下自己写的那棵树的 digest（一个目录自己的事实，不是第四条 journal；**不进 package snapshot** 故 version id 不变），于是四种答案——没有 → seed · 与本二进制逐字节相同 → up to date（顺手补记录，老 store 唯一的补课机会）· 记录仍描述盘上这棵树 = 本 harness 自己写的、没人动过 → **自动刷新成新源码**（连该 draft 下不再提供的文件一起清掉）· 记录对不上或根本没有记录 = 有人编辑过或是记录出现之前的老 seed → **原样留着并点名**，`--force` 是唯一覆盖入口。**为什么要记录**：升级二进制不该让一台机器停在第一次 seed 时的源码上（`agent` 的 `audience`、`evolution` 的 `activation: on_request` 都是这样失效的），而"编辑过没有"没有第二种判法——hash 会被自演化每轮改掉，问一句则跑在开屏之前的后台里；记录**只授予覆盖权**，读不出/不认识/不存在一律落回"别动它"。DESIGN §7.2 / §7.4 / §7.8 / §14。
- **也跑通（vision：user turn 带图片）**：`ledger.Event.user_text` 从裸字符串变成 `{text, images:[{media_type, data}]}`（`data` = base64 文本，ledger 只存不校验），落盘**只在非空时写 `images` 列**——纯文本行与这一列存在之前逐字节相同、老行读回空（`usage?` 的同一套纪律，header `v` 仍是 1）；图片是 **model-visible** 的，所以与 `usage` / `stop_reason` 相反：`prompt.Turn.UserText` 有这个字段、`isStablePrefix` 比它，而 images slice 是**原样借 ledger 的**（没有要改写的东西，也就不需要 `call_storage` 那样的存储）。入口只有 **`nulya session append <id> [<text>|--file f] [--image <path>]…`**（可重复，与文本合成**同一条**事件投 inbox），三道门全在壳层、全在投递之前（内核核心不知道它们存在，与 §9 trust gate 同一先例）：**vision**（读 header 冻结的 `model_identity.model` → `[[models]]` 目录的 `vision = true` 才放行，**没有条目 = 不主张 = 拒绝**，文案指路 config 键与 `nulya config show`；目录只认 trusted 层，checkout 主张不了）· **类型**（魔数 sniff png/jpeg，扩展名不作数）· **大小**（单张原始字节 ≤ 5 MB，报实际大小与上限，绝不替用户缩图）。`session events` 打印时把每张图的 base64 换成 `[image <media_type>, N base64 bytes]` 再重编码（`seq` / `origin` 与其它行原样；`--stream` 的 ledger 行**不**省略——driver 面要与文件同形）。三个 provider 各按自家形状序列化：anthropic 的 image source block（`cacheableBlocks` 与 `writeMessage` 同步计数，断点照常可以落在 image 块上）· openai 带图才变 parts 数组（**不带图仍是纯字符串、逐字节不变**，implicit prefix cache 的键料）· codex 的 `input_image`；data URI 拼接落 `providers/wire.zig`（两个 consumer），scripted 忽略。图片**不跨 fork**（fork 本来就不复制 history），完整回放走 resume。integration 加了第四条（真 PNG → 回答 `red`），只在本机 catalog 标了 `vision = true` 时跑。DESIGN §1 / §3.1 / §3.4 / §9.5 / §13 / §14；契约与进度：`docs/goals/vision.md`。
- **也跑通（T27 的一处内核改动）**：`session step --stream` 在 `model started` 之前先刷**已经存在**的 ledger 行（`StepStream` 拿一个只读 ledger 句柄）——那一刻唯一可能有的就是这一步边界从 inbox 排干的 `user_text`，于是乐观回显的前端当场就知道消息落地了，而不是等整整一个 step（DESIGN §14）。`launch.ScriptedProvider` 多一档 `batch`（一 turn 三个 shell call，给串行 gate 的批量 policy 一个离线替身）。
- **也跑通（gate：每个 tool call 执行前的一票否决 + 三处 CLI 清理）**：内核只多一个原语——`loop.StepContext.gate`（observer 的姊妹：同一个形状、相反的权力，有返回值 `allow` / `deny{note?}`）与 `session step --gate`（**必须与 `--stream` 同用**：请求是那个协议的一行 `{"stream":"gate","event":"request",call_id,tool,args}`，答案是 stdin 一行 `allow` / `deny` / `deny <note>`；认不出的答案、读失败、EOF 一律 **fail closed** 并在 stderr 说一句）。**deny 是那个 call 的 `tool_results`**（`ok=false` + marker 文本 + 人的原话），所以 batch 不变量与事件字母表都没动、模型读得到自己被拒了；deny 只停这一个 call（其余各问各的）、不发 toolBegin/toolEnd、不进 usage journal（`durations_ms` 那一格是 `null`——没有 executor 跑过）；**该不该问是 driver 的 policy**（TUI 的 `/mode` 两档 + `tui.toml [approvals]` 三张表，tui.md §5.7）。manifest 的 `contributes.tools[].readonly?` 是配套的**声明**（与 `permissions` 同级：解析、冻结、不强制，缺省 null ≠ false）。顺手三处清理：`config show --refresh` → **`nulya config refresh`**（`show` 从此一个字节都不联网）· **`ext rollback` 动词删除**（回滚 = `activate` 旧版本，`Store.rollback` 本来就是 `activate` 的别名）· **bare `nulya` 改成打 help**、demo 搬成 **`nulya demo`**（`zig build run` 传 `demo`，冒烟用法不变）。DESIGN §4 / §7.2.1 / §9 / §14。
- **也跑通（background：后台 shell）**：`shell {background:true}` 起一个**活得过 step 进程**的命令，调用立刻返回回执（任务全名 `<sid>/t<N>` + log 路径 + status / wait / kill 三条命令），结束时结果作为**第五种 ledger 事件 `task_finished{task, exit_code, text}`** 经 inbox 在下一个 step 边界进对话（与 `capability_note` 同 genre：跨进程的事实，投影只投 `text`，三个 provider 各按自家形状投成 **user 侧**文本——openai 上**不**跟 note 走 `system`，因为它带着任意进程的输出）。内核只长两块 substrate：`Environment.startShellTask`（第三个动词，`shell` flag 与 `nulya task run` 同一条路；Windows 上 spawn 前后摘掉本进程 stdio 的 `HANDLE_FLAG_INHERIT`，否则 supervisor 继承调用方的管道写端、"后台"根本不成立）与那种事件；**supervisor 与全部动词在壳层 `cli/task.zig`**（`session.zig` / `loop.zig` 零改动）。supervisor = `NULYA_EXE task supervise`：持 `<task_dir>/.lock`、真命令走 `Tree`（kill 杀整棵树）、`kill` 标记每 250 ms 看一次、**先 deposit 后写 done**（看见 done 就 step 的 driver 必须找得到那条事件）。`.nulya/scratch/<sid>/tasks/t<N>/` 五个文件（`status.json` / `output.log` / `.lock` / `kill` / `notify`），**status.json 是真相、`task list` 只是投影**（`starting` = 还没写 status，`lost` = 说 running 但租约空闲）。`nulya task run|list|status|wait|kill|retarget`（`wait` 三个退出码 0/2/3 给 driver 一次分支；`--any` 只把**结果未被读走**的 done 算成 0，否则 driver 的循环停不下来）。**没有缺省 timeout / 没有上限**，取消 step 不碰任务，usage journal 记的是那次发射。kernel prompt 加一句关于 ledger 角色的事实（"只有 user turn 是人写的…"，`kernel_hash` 变一次）——**只加卫生不加边界**，边界仍是 gate 与将来的 sandbox。DESIGN §3.1 / §3.4 / §4 / §6.1 / §7.5 / §8 / §13 / §14；契约与进度：`docs/goals/background.md`。TUI 的任务面见上面 TUI 那条的 T29（内核零改动）。
- **也跑通（`edit` 搬出内核：builtin 从两个变成一个）**：判据是本文件那把尺子——删掉 `edit`，八条 physics 一条都不失效（authority 上 `edit ⊆ shell`，没有它照样 `nulya ext build`）；删掉 `shell` 则什么都造不出来。所以 `edit` 成了 `extensions/std` 的**第六个 tool**（DESIGN §6 / §7.8），按 D7 逐条移植 tcode `fs/edit.rs`：精确匹配短路 → 标点 / 逐行空白 / 跨行 reflow 三级归一回退（每级只在**唯一**命中时动手、回填文件真实字节、多于一个候选一律报歧义）、CRLF 文件收 LF `old_string`、`replace_all`、`target_line`（与 `replace_all` 互斥）、歧义 ≤ 5 个带行号候选、not-found 给相似行提示（没读过该文件再附一句 note）、原子写保留可执行位、回显以替换点为锚的带行号片段并按新 hash **`record_read`**（不是 write——write 会把整文件标成看过）。**内核因此少了四样东西**：`src/tools/edit.zig`、`registry` 的第二个 builtin、`manifest.reserved_tool_names` 的第二个名字、`environment.WorkspaceFs` 与 `ToolContext.fs`（没有 in-core tool 再读文件了）；kernel prompt 那句改成 "shell 是那一个永久 builtin"，`kernel_hash` 因此变一次（纯 provenance，老 session resume 警告一行照跑）。**收益**：base-tools.md 列的 later hardening 从此是普通的 extension 版本 bump，而 D4 那条已知代价（内核 edit 不登记 freshness → edit 后 write 被拦）**消失**——六个 tool 共用同一份记录，e2e 的钉子改成 read → edit → write 通过。TUI 侧只是计数（`tools 1+N`）、`std_pins` 六个、老 `session_pins` 一次性补 `ext:std/edit`（tui.md T30）。
- **也跑通（credential 的第三处来源 + `session new` 缺凭据硬失败，内核两处改动，DESIGN §9.5/§14）**：① **`<NULYA_HOME | ~/.nulya>/credentials.toml`**——键是 profile 已声明的 `api_key_env` 那个名字，值是 key；顺序 `config > env > file` 只在 `launch.credentialSource` 一处定义（改它，`config show` 的可用性投影 / `session new` 的冻结 / resume 全跟上）。**为什么需要它**：每个子进程的 env 都被 `isSecretKey` 剥掉 secret（physics #6，不改），所以后台任务与 driver 型 extension 解析不出 `api_key_env`——而 `codex` 从来没这问题，正因为它的凭据一直是**文件**；这是把那个先例推广给其它 provider，profile 配置一个字不用改。TOML 而不是第四条 journal（三条 jsonl 记的是发生过的事或一次授权，这个是人写的设定，与 `config.toml` 同类同解析器）；**值绝不进任何投影**；POSIX 上 mode 宽于 0600 → 每进程至多一行 stderr 警告、照读（`auth.json` 同款态度）。② **`session new` 缺凭据 exit 1**：profile 点名真实 provider 而三条路都解析不到 → stderr 指路（变量名 · `credentials.toml` 绝对路径 · user config · `nulya config show`）+ **什么都不创建**。它曾经是"警告一行然后把身份冻结成 scripted"——比失败更糟的一种失败：场开起来了、看着就是被点名的那个模型、回答它的是离线替身，且身份是冻的、这一场一辈子如此。现在与 resume 的 `MissingCredential` 对称。**唯一例外是 `nulya demo`**（不带 key 跑就是 demo 的语义）：`createSession` 收一个 `KeylessPolicy{refuse, stand_in}`，两个调用点各自写明要哪个。
- **也跑通（M6 Phase A 的 fact 那半：usage fact 多一列可空 `version`，DESIGN §5.5）**：`.nulya/tool-usage.jsonl` 的每条 fact 多记**这次调用由哪个冻结实现服务**（`v-<hash>`），`tool_id` 照旧不带版本——**双身份**（PLAN §3.5.1）：一个 tool 的历史是**一段**历史，而同一段历史也能按实现读（上次重建之后是不是变差了）。**仍是 `v:1`**：这条 journal 的纪律一直是"加可选列、reader 忽略未知列、同 `v` 的新写者不破坏老读者"（`at` / `session` / `duration_ms` 三个先例），升 v2 只会让老读者对每条新行报错、零收益。两个写点各自拿着答案、零新 plumbing：`session.recordCompletedToolStats` 从**本场冻结的成员列表**（`composition.extensions` 的 `FrozenExtension{id,version}`）按 `ext:<id>/<tool>` 的 `<id>` 反查（**不给 `Binding` 加字段**——版本是冻结成员关系的属性，唯一真相在那里），`cli/ext.zig` 的 `ext run` 用它自己刚解析出的那个版本。null 两种都诚实：老行 = unknown、builtin = 它就是内核。**做这件事的理由是 evidence 补不了课**（journal 只能 append，今天不记就永远 unknown），所以**只写不读**——内核新增读者数为零、`aggregate` 一字未动，per-version 投影等第一个真实 consumer（PLAN §3.5.2）。契约与进度：`docs/goals/M6a.md`。
- **也跑通（`activation` 删除 + `[extensions] with` + `--bare`，2026-08-23 ext-review-2 Lane K，契约 `docs/goals/ext-review-2.md`，DESIGN §5.1/§7.2.1/§7.5/§9.5/§14）**：manifest 的 `activation` 与 fresh 路的 **discovery 一起删掉**（`composition.resolveActiveExtensions` 不存在了）。`activate` 从此只做一件事——**原子改 `current`**，一场 session 都不改变；"这个包进不进 session" 移到人这一侧，与 pin **逐位对称**的一根轴：config `[extensions] with`（每一场；**project 层也读**——它只能在已持有已信任的包里挑，不像 `extensions.paths` 那样决定哪些目录可以供出代码）与 `session new --with`（这一场）。**2×2 从此是全部**：成员（config `with` / `--with`）× 工具面（`pinned_native_tools` / `--pin`），pin 蕴含成员那条不变。理由两条：reach 是人的决定不是包作者的（physics #6），而 pin 蕴含成员之后作者那个 bit 已经拦不住任何东西（config 一条 `pinned_native_tools = ["ext:plan/propose"]` 就把 `on_request` 的包带进每一场）。**`session new --bare`** 两张常驻表都不读、只按 argv 组合（`max_tools` 照读——天花板不是选择；header 不记这个 flag），第一个 consumer 是 `extensions/agent` 委派出的子场——定义里的 `pins` 就是它的全部工具面。老 manifest 写了 `activation` 的：parse 当未知键忽略，`ext build`/`ext sync` 在 stderr 提一行（`Manifest.legacy_activation` 的唯一读者）。`ext list` 第二列语义就是 `current`（没有就打 `(no current)`）、多一列 `[with]`；`config show` 多投影 `extensions.with`；`--with <id>` 撞上坏掉的 `current` 是 `ActiveExtensionBroken` 并点名（"没建过"仍是 `WithVersionNotFound`，两种修法不同）。TUI 侧见 tui.md T48/T50（T47 说的是 `/sessions` 那张表，不是这条规则住的地方——T48 把 `/ext` Enter 写成"activate + standing pins + standing with"三份，T50 把 standing with 那一份从贡献了 system prompt 的包身上收回：Enter 对这样的包只移 `current`，换来的是 `/<id>` 这条按场戴的命令，不再是"每一场都戴"）。
- **也跑通（tui-plugin：extension 长出前端面，DESIGN §7.2.1 / tui.md T39–T41 / goals/tui-plugin.md）**：manifest 多四个**声明位**（与 `readonly`/`audience` 同纪律：kernel 解析、冻结、validate 管形状、**零 enforce**，消费者全是 driver）——`contributes.commands[]{name,description,action}`（包给驱动者的斜杠命令；`action` 是**恰一个键的对象** `{with:true}` / `{run:"<tool>"}` / `{skill:"<ref>"}`，词表开放、认不出的动词归 reader，`run` 的包内引用 validate 查；ext-review-2 之前是字符串小语言，认一个版本期）· `contributes.policy{readonly?}`（成员期间请求审批收窄；**一个可选 bool，形状自己保证它只能收窄**——从前是三个字段外加一条「没有 `allow`」的 parse 规则，physics #6；空 `{}` 不算贡献）· `tools[].ui{render?,panel?}`（画法提示，开放词表；2026-08-23 前是两个顶层字段）· `contributes.ui{"<host>":{entry,api}}`（**按宿主键**的前端模块，`"tui"` 是这个仓库那个前端的键，entry 随版本冻结，**kernel 从不加载它**；先叫 `tui`、后是平铺的 `{entry,api}`，平铺形认一个版本期）。TUI 两层消费：**声明层**（T39）——commands 进 slash 链（内建 → 包 → skill，内建永不被夺走）、policy 复用 agent readonly **同一个**天花板（deny note 点名是哪个包）、`render:"checklist"` 卡 + `panel:true` 投影成输入框上方 PanelStrip（纯 ledger 投影，重放与 live 同一条带子）；**代码层**（T40）——`tui/plugin-api.d.ts` 是唯一契约（同主版本只增不减，认不出的 api 号 warn-and-skip），trusted 且 activated/worn 的包的 entry 运行时 `import()`（`bun build --compile` 单文件里实测可用，dev/compiled 同一条路；Bun 1.3.5 首败模块 re-import 挂死 → 失败条目永不重试），**行渲染不暴露 OpenTUI/Solid**（`render(width)->Line[]` + theme token，NO_COLOR 白拿），五个注册面（command / card **只许本包 tool** / panel（composer 区、宿主画 `◈` 归属行、审批框与 mode picker 与贴 key 这些 trusted zone 在场时 open 只排队）/ widget（压过同包的 `panel:true` 投影）/ observe 只读），actions **只有人已有的动词**（appendNote 的 `<ext-note>` sentinel · extRun 限本包 · openTab · wearNext · 1.1 的 compact），`tui.toml` `[extensions] plugins=false` 一键回到纯声明层。**两个 consumer**（T41，第七、八个自带扩展）：`extensions/plan`（persona + `policy readonly` + `/plan`=with（**manifest 不写这条命令**——贡献 prompt 的包自动得到自己名字那条，T49）+ `propose{plan_md}`/`todo{items}`（handoff 形状：立即返回、计划与清单都活在 **args=ledger** 里）/`approve`（driver audience）+ 评审面板——propose 结束自动开、`j/k`/`v` 选区/`c` 评论、`r` 把评论拼**一条** sentinel user turn 回模型（append-only 命中前缀缓存）、`a` = extRun approve 写 brief → `actions.compact({briefFile})` fork 到**不戴 persona** 的执行场）与 `extensions/ask`（`ask{question,options?,free_text?}` + 选项面板、答案 sentinel append；没插件时问题就在 ledger 的 tool 卡里、打字即答——降级即对话）。tcode 的 plan / progress / askUser 至此全部是**可关可换的包**：不装，连 `/plan` 这个词都不存在。
- **也跑通（session-prompt：per-session system prompt 冻进 header，agent persona 去材料化，DESIGN §3.4 / §5.6 / §14）**：`session new --prompt <file>`（可重复）在**创建时读字节**、冻进 header 的 `composition.prompts`（`{source, text}`；缺省空表故老 header 读回空，header `v` 仍是 1），resume 从 header 读回、**不经 store**——`composition` 的 fresh 与 frozen 两条路在这一样东西上都不查 root（e2e 钉死：一个只有 `--prompt` 的场，把整个 store root 目录删掉后照样 resume 出 byte-identical 的 system blocks）。block 顺序 **kernel → extension → inline → `skills:catalog`**；`source` = 文件 basename 去扩展名，**内核从不解释它**（不去重、不加前缀、不排序）；缺文件 / 空文件 / 超 `prompt.max_system_prompt_bytes` → stderr 点名 + exit 1、**什么都不建**；fork **不继承**（与 `--with` 对称）；`session list --json` 只投影 `source` 与字节数、不投正文。**尺子**：这段文本有没有独立于某一场 session 的生命周期——有（evolution / handoff / plan：装得上、activate 得了、回滚有意义）→ extension，没有（persona 正文、将来任何 per-session brief）→ `--prompt`（PLAN §3.2 那句"被 `--with` 吸收"只对前者成立）。**第一个 consumer 是 `extensions/agent`**：`materialize` 改名 **`render`**（只把定义正文写成 `.nulya/scratch/agents/agent-<name>.md` 并回一整组 `session new` 参数，不再 `ext build`、不再有版本），委派与 `/agent` 都改传 `--prompt`；四道门 / 追问形态 / readonly runner / 委派白名单 / depth 兜底一律不动，只有 `wornPersona` 改读 header `composition.prompts[].source` 的 `agent-` 前缀——那个前缀从此**只是这个包自己的写/读约定**（写者读者同一个包），kernel 侧没有任何 `agent-` 字样。**收益**：`/ext` 的派生包污染从源头消失（不是被藏起来），且 persona 场的 resume 不再与 `ext prune` 耦合（`ext prune agent-explore` 从前会弄断冻在旧版 persona 上的场）。TUI 侧只跟随（`sessionNew`/`SessionExtras` 多一个 `prompt`、`renderAgent`、`wearing()` 多读 header 的 inline prompt），`/ext` 零改动。契约与进度：`docs/goals/session-prompt.md`；TUI 侧见 tui.md T44。
- **也跑通（ext-review，2026-08-23：extension 面的六处收口，契约 `docs/goals/ext-review.md`）**：对 extension 参数面做了一次整体评审，结论是内核没破、四不像在三处——manifest 三种听众混在一张表、"能力怎么到模型面前"散在七个概念里、脚本 extension 名存实亡（6/6 有 runtime 的自带包全是编译 Zig）。四条 lane 落地：**A** `runtime.wire: "plain"`（stdin 参数 JSON、`NULYA_TOOL` / `NULYA_ARG_<k>` 进 env、stdout 原文即结果、退出码即 ok；两种 wire 同一条 `runExtension` 路，契约写在 `protocol.zig` 模块注释顶部所以 `ext api protocol` 零漂移）+ `entry` / `interpreter` 可按 `builtin.os.tag` 写成对象（只许脚本 kind，宿主选不中是 `EntryUnsupportedOnHost`）+ `ext init` 缺省脚本（`run.sh` + `run.ps1`，`--zig` 才编译）；**B** gate 请求行多 `tool_id` / `readonly` 两列（`ToolDefinition.readonly` 从冻结 manifest 经 `Binding` 投影，`ToolGate.Request{call, definition?}`；observer 不动、不带 `--gate` 逐字节不变）→ `extensions/agent` runner 与 TUI 审批不再各自从 manifest 重推，+ **pin 蕴含成员**（`composition.resolveFreshExtensions`：pin 的包不是成员就按 `current` union 进来；没有 `current` 仍拒并点名 pin）；**C** `activation` 缺省按形状、`ext run` 不套 timeout（`--timeout-ms` 可选）、`ext inspect` 只答 store（`<id>` = 生效版本、`<id>@<v>`、`<path>` = draft）；**D** manifest 按听众分层——`contributes.tui` → `ui`、`tools[].render/panel` → `tools[].ui{}`、`wear` → `with`、`permissions` 出八个自带包与两个模板（字段当时留着等 M7 沙箱，ext-review-2 的 M1 把它整个删了）、空 `policy{}` 不算贡献，`ext api permissions`（今名 `manifest`）与 DESIGN §7.2.1 重写成"内核强制 / driver 声明 / 前端声明"三层、每层纪律只说一次。**两根轴的 2×2 从此是全部**：成员（activate 每场 / `--with` 这场）× 工具面（`pinned_native_tools` 每场 / `--pin` 这场），其余（`audience`、`activation`、TUI 的 `/with`、`/ext` Enter）都是在这张表上走格子。内核 physics 零改动，`kernel_hash` 不变。DESIGN §4 / §5.1 / §7.1 / §7.2.1 / §7.3 / §7.5 / §7.8 / §14。
- **也跑通（manifest 瘦身，2026-08-23 ext-review-2 Lane M，契约 `docs/goals/ext-review-2.md`，DESIGN §7.2.1/§7.8/§9/§14）**：三层叙述不变，字段少了三个、形状换了两个。**`permissions` 删除**——`{fs, network, process}` 解析了、冻结了、**零读者**，留着等沙箱；一条没人执行的声明放久了会被读成保证，而沙箱真到时（PLAN §3.8）要什么形状该由它自己定。**`policy` 只剩 `readonly: ?bool`**：`deny` / `ask` 两张表没有 `readonly` 答不出的用例，而删掉它们之后「包只能收窄」由**形状**保证，不再需要那条「`allow` 键一出现就在 parse 拒」的规则（`PolicyAllowNotPermitted` / `InvalidPolicyEntry` 一并消失，`allow` 从此只是未知键）。**`commands[].action` 从字符串小语言变成恰一个键的对象** `{with:true}` / `{run:"<tool>"}` / `{skill:"<ref>"}`（键是动词、值是它的参数或 `true`；零个或两个键是 `InvalidCommandAction`，`run` 的包内引用仍由 validate 查）。**`contributes.ui` 按宿主键** `{"tui":{entry,api}}`（host 是开放词表 `[a-z0-9-]+`，内核 schema 不该点名一个具体前端；`validateUi` **每一条都查文件在不在**——一个版本要服务所有宿主，build 是唯一能发现「某个宿主的模块没写」的时刻）。**driver tool 的 `timeout_ms` 删**（`agent` 的 `render`/`list`/`run`、`compact`）——那个字段只在模型面生效，写在从不被 pin 的 tool 上只会让人以为委派有天花板。老形状都认一个版本期，各由一个 `legacy_*` bool 喂 `build_ext.noteLegacyShapes` 的一行 stderr（K 的 `noteLegacyActivation` 先例，四个 consumer 到了才收成一处）。`ext api permissions` **改名 `manifest`**（旧名保留一个版本期、打同一段）。**TUI 侧一处新行为**：贡献 system prompt 的包**自动**得到 `/<id>` = with（`extensions.derivedCommand`），所以 `extensions/plan` 的 `commands` 条目删掉了，而 `ask` 的留着——它不贡献 prompt，`/ask` 是它自己的主张；包自己声明的同名条目优先，内建名永不被夺走（既有规则）。tui.md §11 T49。
- **也跑通（一种 wire，2026-08-23 ext-review-3 Lane W，契约 `docs/goals/ext-review-3.md`，DESIGN §7.1/§7.3/§7.8）**：六个自带编译包（`std` / `agent` / `plan` / `handoff` / `compact` / `ask`）**全部迁到 `plain`**，manifest 各加一行 `"wire": "plain"`，tool 逻辑一个字没动。每个包的 `main` 从此是「读 stdin 的参数对象 → 按 `NULYA_TOOL` 分发（单 tool 的包忽略它）→ stdout 原样是结果、exit 0 / stderr 是消息、exit 1」，删掉的是读信封、回 id、拼信封那一半（`rpc.zig` 三份各去掉信封与 `Request`/`fallback_id`，`handoff`/`compact`/`ask` 三处内联信封整个消失）。**退出码不做词表**：`-32602` / `-32601` / `-32000` 三个码到模型那里只是一个没人分支的数字，所以 `Fail{code,message}` 塌成一个 message、`invalidParams` 与 `refuse` 并成一个 `refuse`、`Outcome.json` 与 `.text` 并成一个 `.text`（plain 上两者都只是「把这些字节打到 stdout」）。**模型看到的字节只变了框**：成功逐字节相同（driver 面的 `render`/`list`/`compact`/`handoff` 照旧打同一个 compact JSON，TUI 与 `drivers/goal.*` 零改动），失败从 `extension error [-32000]: <msg>` 变成 `exit 1` + stderr 的同一句 `<msg>`。**一处新纪律**：plain 上 **stderr 就是失败消息**，所以包必须独占它——`extensions/std` 因此给 vendored mvzr 装了一个空 `std_options.logFn`（否则 `error(mvzr): …` 会挤在教学文案上面）。`jsonrpc` **标 deprecated**（`protocol.zig` 顶部、`ext api manifest`/`examples`、DESIGN §7.3 多一段「为什么退场」）：仍是缺省、仍认一个版本期，给仓库外写的扩展；删 `ToolCallRequest` / `decodeResponse` / `invokeJsonRpc` / `Wire.jsonrpc` 是下一条 lane。
- **还没有**：自动压缩触发（未实现，也没有对应的 config 键——何时压是 driver 的 policy）；沿 parent 链呈现连续对话（`session list --json` 的 `root` 已经算好，前端还没连）；handoff 的 driver 守卫（context 阈值 / brief 长度，第一版 `drivers/goal.*` 故意一条都没做，等真实使用证据）；TUI 的 `/goal`（tui.md T10 占位）；subagent（= session 自调用，缺第一个 consumer）；policy hook（未实现，也没有对应的 config 键）；sandbox / remote environment；first-party Anthropic key 上的实测（cache_control 采纳、thinking-on tool 循环——integration 第三条已写好等 key）；`session new --budget-tokens`；persistent extension runtime。这些的去向都在 PLAN.md。

## 模块表（`src/`，扣掉同文件测试约 6k 行）

| 文件 | 职责 | 关键不变量 / 备注 |
|---|---|---|
| `main.zig` | 入口：一律 `cli.dispatch`（无参数 = usage 一屏；demo 是 `nulya demo` 一个动词） | 注册 host environ，其余全在 `cli/` |
| `ledger.zig` | 4 种事件（`user_text` / `assistant{reasoning,text,calls,usage?,stop_reason}` / `tool_results[]` / `capability_note`；`reasoning` 是 provider 原样的 reasoning item 数组、不透明、kernel 不解析、只交回同一 provider 回放；`StopReason` 声明在这里、`provider.zig` re-export，落盘只写 shape 说不出来的 `max_tokens`/`other`，老行的 `truncated:true` 仍读得回但不再写），deep-copy 所有权（快照进 ledger 自己的 `arena`——append-only + 整体释放 = 一个生命周期，所以没有 per-shape 的 clone/free 链；`append` 返回后调用方仍可自由释放入参）；durable 文件（header 是 `Header` 的 `std.json` 类型化编解码——**字段名就是落盘键名**，所以 `FrozenComposition.active`（其实是全部成员 extension，含 `--with` 的）这个 v1 遗留键名只能等下次升 schema 再改 + `seq` JSONL，header 冻结 composition + `model_identity` + `nulya{version,kernel_hash}` provenance stamp + `composition.prompts`（`InlinePrompt{source,text}`，`session new --prompt` 冻进来的 per-session system prompt **字节**；缺省空表故老 header 读回空、header `v` 仍是 1））；跨进程 inbox（`depositEvent` 原子投递 / `drainInbox` 排干，事件带 `origin` 投递去重列 → 应用 exactly-once） | 唯一写口 `append`；`init` 纯内存 / `createDurable`+`openDurable` 落盘（一文件=一 generation、**一个写者**由 `<id>.lock` 排他 advisory 锁独家强制 → `SessionBusy`；ledger corruption 交给 replay/seq/JSON 校验） |
| `prompt.zig` | `Ledger → PromptIR{system_blocks, turns}` 纯投影（一个 ledger 事件一个 `Turn`，turn 不拆散；call / result 是 PromptIR 自己的 `ToolCall`/`ToolResult`，`usage` / `stop_reason` / `spill_path` / `origin` 在类型里没有字段，所以不可能被投影） | `isStablePrefix` 仍是缓存不变量的可测形式；turns 借 ledger 的字符串，不比 ledger 活得久；**可回放性住在这里**：`stop_reason == max_tokens` 的 turn 上不是完整 JSON 值的 `args_json` 投影成 `{}`（ledger 那边照记原样）；generation == 文件（`currentGeneration` 已删） |
| `loop.zig` | 一次 step：freeze snapshot → `collectTurn`（每次尝试新建 collector；瞬态故障按 `StepContext.retry` 指数退避原样重发，observer 收 `modelRetry`）→ 串行执行 batch（每个 call 先过可选的 `StepContext.gate`，gate 拿到的是 `ToolGate.Request{call, definition?}`——冻结的 `ToolDefinition` 随问题一起给）→ 一条 tool_results | 取消时补齐整批（三种 marker）；gate deny = 第四种 marker（`ok=false`、不发 toolBegin/toolEnd、不进 usage journal、**只停这一个 call**），不设 gate 逐字节不变；`max_tokens` 截断的 turn 照记 calls（原样）、不执行、marker 批次关掉（`StepOutcome.stop_reason`），torn 参数由投影换成 `{}`；未知 tool 的错误文本列出本场冻结的整个工具面；`completeInterruptedToolBatch` 修复上次残尾 |
| `session.zig` | `AgentSession`：ledger 生命周期（`init` 内存 / `createDurable`+`openDurable` 落盘，resume 时 composition 从 header 冻结重建）+ step 边界（补残尾 → 消费 `<id>.cancel` → 排干 `<id>.inbox`）+ `run` 预算 + usage 记账 | `max_steps_ceiling` 由 kernel 夹；`requestCancel` 是跨进程取消的唯一入口（durable session 才有 siblings） |
| `composition.zig` | session 开始冻结 tools / skills / system prompts / 成员 extension 的版本（`FrozenExtension`）；两阶段（`resolve` 决定成员 + pin → `assemble` 造冻结状态）共用一个 composition arena，`deinit` = 释放它；`Options.prompts` / `FrozenComposition.prompts` 两条路同归 `buildSystemPrompts`（block 顺序 kernel → ext → inline → `skills:catalog`，DESIGN §5.6） | 两根轴分开：**版本冻结** ≠ **pin**（pin 只指 native 工具面），但 **pin 蕴含成员**（`resolveFreshExtensions`：pin 的包不是成员就按 `current` union 进来，没有 `current` 仍是 `WithVersionNotFound`）。inline prompt 是第三样东西：**不查 store**（fresh 路来自 argv、frozen 路来自 header），`source` 原样透传、内核从不解释。成员解析两条路（被点名：config `[extensions] with` ∪ `--with` ∪ pin 蕴含 / header 冻的 `active`；**discovery 已删**）**一律硬失败**：不带版本时 `current` 坏掉 → stderr 指名 + `ActiveExtensionBroken`，没有 `current` → `WithVersionNotFound`（两种修法不同，host fault 照原样传播）；pin 解析不到 → `PinNamesUnknownExtension` / `PinToolNotDeclared`；`max_tools` 含 builtin |
| `registry.zig` | `ToolSetSnapshot`：builtin 固定最前，extras 按稳定 id 排序，name/id 唯一 | |
| `tool.zig` | `ToolExecutor{ptr,vtable}` / `ToolDefinition{id,name,description,input_schema,readonly?}`（`readonly` 是冻结 manifest 的声明、不进 provider wire 也不进 `kernel_hash`，只给 gate 看） / `ToolContext{environment,cwd}`（`fs` 随 builtin `edit` 一起删了——没有 in-core tool 再读文件） | tool 拿不到 ledger；extension 子进程只拿 request + 净化 env + cwd |
| `tools/shell.zig` | **唯一**的永久 builtin（`edit` 已搬进 `extensions/std`，DESIGN §6/§7.8） | `shell` = `{command, cwd?, timeout_ms?, background?}`：前台默认 120s / 上限 600s 来自 `tool.Timeouts`，超时 kill 并返回已捕获输出；**`background:true` 走 `startShellTask`**（DESIGN §6.1）——`timeout_ms` **原样递、不夹、无缺省**，回执立刻返回，无 session 就 `ok=false` + 教学文案且什么都不启动，非 bool 的 `background` 与非正整数的 `timeout_ms` 同一条拒绝纪律 |
| `emit.zig` | 统一输出原语：head/tail 字节预算、UTF-8 边界、超限落盘留指针；step 聚合预算只约束正文，每个结果保底一条完整 footer 指针（顺序不决定可见性） | 落盘在 `.nulya/scratch/<session-id>/`（`launch.sessionScratchDir`），文件名按 ledger seq——session 内 replay 一致、并发 session 不碰撞 |
| `environment.zig` + `environment/tree.zig` | `Environment{runShell, runExtension, startShellTask, dialect}`；`LocalEnvironment`（`LocalOptions.session: ?SessionRef{session_path, tasks_dir}` 由壳层给——core 不 import launch，"放哪儿"是壳层的决定，与 `StepContext.scratch_dir` 同一分工）；进程树 spawn/kill 与有界等待（`Tree` / `waitBounded`）在 `tree.zig`；**没有 `WorkspaceFs`**——它只为 builtin `edit` 存在，`edit` 搬进 `extensions/std` 后一个读者都没有了（DESIGN §8） | `startShellTask` = 独占 mkdir 取第一个空 `t<N>` + plain spawn `NULYA_EXE task supervise`（stdio 全 `.ignore` / `create_no_window` / POSIX `pgid=0` / cwd 是 workspace），**不 wait**；Windows 上 spawn 前后摘掉本进程 stdio 的 `HANDLE_FLAG_INHERIT` 再还回去（`DetachedStdio`——`CreateProcessW` 是 `bInheritHandles=TRUE` 无 handle list，否则 supervisor 继承调用方的管道写端，调用方的 drain 要等后台命令跑完，实测 30 s）；`shellArgv` 是 argv 决定的唯一实现（前台 `shell` 与 supervisor 两个 consumer）；子进程 env 走 `isSecretKey` denylist 净化，并**加一个 `NULYA_EXE`**（本进程可执行文件绝对路径，让 driver 型 extension 调得到"正在跑的这个" nulya；取不到就不设，init 永不因此失败。`NULYA_SESSION` 由 `session step` 另加）；`runShell` 的超时是 `child.wait` 与一个 sleep 任务在 `std.Io.Select` 里赛跑（取消点仍是 `child.wait`，io 给不出并发单元就裸跑）；超时与取消都杀**整棵进程树**（`Tree`：POSIX `pgid=0` + 负 pid 信号 / Windows 无 limit 的 job object + `TerminateJobObject`，OS 拒绝就降级只杀直接子进程），否则孙进程攥着管道写端让 drain 等不到 EOF；**正常返回不杀**，后台进程两平台一致地活下来 |
| `provider.zig` | `Model{ptr,vtable{stream}}` + `TurnCollector` + `Usage`/`StopReason`/`StreamEvent` + `RetryPolicy`/`isTransient`（provider 一次 `stream` 只试一次、把故障归类；重试循环在 loop） | provider 只能优化序列化，不能破坏 §1 turn 前缀；`reasoning_item` 是完整的 provider 形状 item，collector 原样收成 `ModelTurn.reasoning`，`thinking_delta` 只供展示 |
| `providers/wire.zig` | 三个真实 provider 共享的 wire 底座：`postSse` / `postJson`、assistant turn 的 `reasoning` 拆回逐个 item（`writeReasoningItems`）、JSON 标量；故障归类：线路故障折成 `Transport`，状态码分 `Unauthorized`/`RateLimited`/`ServerError`/`ApiError`；`Watched` stall watchdog（字节级静默超 `Post.stall_ms` 就 cancel 交换任务报 `Transport`） | SSE 行用可增长缓冲（一行能装下整个 response 对象）；`event:` 行忽略——三种方言都把事件名写在 payload 里 |
| `providers/openai.zig` | chat/completions（OpenAI / DeepSeek / 兼容端点），implicit prefix cache | 读 `cached_tokens` / `prompt_cache_hit_tokens`；DeepSeek 端点（`isDeepSeek`）：`off` → `thinking:{type:disabled}`，本轮 `reasoning_content` 收成一个 `reasoning_item` 并在带 `tool_calls` 的 assistant message 上回放（文档：不回放即 400） |
| `providers/anthropic.zig` | Messages API（含 DeepSeek `/anthropic`），**explicit cache breakpoints** | 同 role 块合并成一条 message（一批 tool_results = 一条）；usage **合并**不覆盖，否则收尾事件清零 cache 计数；thinking block 整块收进 `reasoning`、回放在 `tool_use` 之前（thinking 开着时不回放即 400） |
| `providers/codex.zig` | ChatGPT 订阅的 responses 端点 + `~/.codex/auth.json` OAuth（401 refresh 回写） | cache key = hash(session id)，跨进程稳定；`include: reasoning.encrypted_content` 要回加密 reasoning item 并回放（已实测接受） |
| `config.zig` + `default.toml` | `default → system → user → project` 合并；project 层过 `mergeProject` 只能收窄；两张表描述模型：`[[provider.profiles]]`（怎么连 + 服务哪些 `models[]`）与 `[[models]]` 目录（一个 id 是什么：label / efforts / default_effort / context_window，按 id 合并、只认 trusted 层）；`[provider.retry]`（全 profile 一份的重试策略 + `stall_timeout_ms`，trusted 层）；`Config.defaultEffort`；`[extensions]` 两个键两条相反的规矩——`paths`（store root）**只认 trusted 层**（它决定哪些目录可以供出代码），`with`（常驻成员名单，DESIGN §5.1）**project 层也读**（与 `pinned_native_tools` 同理：只能在这台机器已持有、已信任的包里挑，引不进代码） | 用 vendored `zig-toml`；目录是纯描述，kernel 不读 |
| `<NULYA_HOME \| ~/.nulya>/credentials.toml` | credential 的**第三处**来源（`launch.fileValue`）：键就是 profile 已经声明的 `api_key_env` **那个名字**，值是 key。`launch.credentialSource` 是定义顺序的唯一一处：`config > env > file`（codex 仍是 `auth.json`） | 存在的理由是子进程拿不到 secret（`isSecretKey` 剥掉，physics #6）——后台任务 / driver 型 extension 因此解析不出 `api_key_env`，而 `codex` 从来没这问题正因为它的凭据是**文件**；这是把那个先例推广开。TOML 不是第四条 journal（三条 jsonl 记的是发生过的事，这个是人写的设定）；**值绝不进任何投影**（`config show` 只多一档 `credential_source = "file"`）；POSIX 上 mode 宽于 0600 → 每进程至多一行 stderr 警告、**照读** |
| `extension/manifest.zig` | `nulya.extension/v2`，三层：内核强制 `runtime?{entry, interpreter?, wire?}`（`entry` / `interpreter` 可按 OS 写成对象，`PlatformValue`）+ `contributes{tools[{name,input,timeout_ms?}],skills,system_prompts}`；driver 声明 `tools[].readonly? / audience?`、`policy{readonly?}`；前端声明 `commands[]{name,description,action{<verb>: true\|"<arg>"}}`、`tools[].ui`、`ui{"<host>":{entry,api}}` | manifest 是 schema 唯一真相，不问 binary；`isScript` = entry 非 `bin/`；`timeout_ms?` = 这个 tool 自己的 wall-clock 上限（缺省 30s，上限 `tool.Timeouts.extension_max_ms` = 600s，越界 `InvalidTimeout`），**只在模型面生效**所以 driver tool 不写它；`readonly?` 是**声明**（kernel 解析、不强制，缺省 null ≠ false），消费者是 driver 的审批 policy；`action` 恰一个键（`InvalidCommandAction`）、`ui` 的 host 键是开放词表（`InvalidUiHost`）；**manifest 说不出"我进哪些 session"**——那是人的决定（DESIGN §5.1）。四个老形状认一个版本期、各由一个 `legacy_*` bool 喂 `build_ext.noteLegacyShapes` 的一行提示：`activation` / `permissions`（当未知键忽略）、`action` 的字符串小语言、平铺的 `ui{entry,api}`（读成 `tui` 那一条）
| `extension/protocol.zig` `invoke.zig` | oneshot spawn-stdin-stdout-exit。**要写的 wire 只有 `plain`**（stdin 参数 JSON + `NULYA_TOOL`/`NULYA_ARG_<k>` env，stdout 原文即结果，退出码即 ok）；`jsonrpc`（缺省、`tool/call` 信封）**已 deprecated**、认一个版本期。契约在 `protocol.zig` 模块注释顶部 | 两种 wire 同一条 `runExtension` 路；jsonrpc 的响应 id 必须匹配 |
| `extension/store.zig` `roots.zig` `integrity.zig` | `<id>/versions/v-<hash>/{extension.json,package/,bin/}` + `current` 文件 + `<id>/.lock`（`Store.lease`：build / activate / deactivate 的写者 lease）；`roots.Roots` = 有序 root 搜索（首个 active 持有者胜） | version = hash(snapshot + compiler + target)，`compiler`+`target` 仅 compiled kind 非空（data/script 纯 snapshot、免 zig，见 `manifest.ImplementationKind`） |
| `extension/build/{build_ext,toolchain,templates}.zig` | `nulya ext build`：冻结 snapshot →（`bin/` entry）`zig build-exe` frozen `src/main.zig` /（`src/` entry）脚本直接冻结不编译 → seal；编译前先在 `donors`（别的 store root）里找同一份 snapshot，找到就复制 + 再验（`buildExtensionReusing`）；`Mode.plan` = 同一份计算、写之前停手（`ext sync --dry-run`） | 内嵌 Zig 0.16 由 `-Dembed-toolchain` 门控，**没内嵌也认 managed 目录里已有的 0.16**（`<data>/toolchains/zig/0.16.0/`，DESIGN §10；`ext build`/`sync` 撞墙那句把该目录的绝对路径写在里面）；脚本 build 不需 zig；没有 zig 时按 seal 的 `(package_digest, target)` 匹配，真要编译才报 `ZigVersionUnreadable` |
| `extension/tools.zig` `skills.zig` `notes.zig` | extension → `Tool` binding / skill catalog / mid-session `capability_note` 的**文本**（投递用 `ledger.depositEvent`，排干在 `session.prepareStep`） | |
| `skill.zig` | `SkillSetSnapshot` + `<available_skills>` 渐进披露文本 | Agent Skills 兼容（`SKILL.md` frontmatter） |
| `journals/journal.zig` | 三条 journal 共用的文件层：一行一条 append（全程持 `<journal>.lock` 排他 lease——多进程共写；建的是 journal 自己那个父目录，所以也能装 user 层的裸文件名）、append 前修 crash 残尾、读端不拿锁且忽略残尾、文件缺失 = 还没有事实；也是共用的**时钟** `rfc3339Now`（三条 journal 的 `at` + header `created`） | 只抽 IO 与时间戳，不抽 `Journal<T>`——schema 各自持有。目录不存在意味着什么由各 journal 自己定（workspace = host fault；user 层的 trust = 还没记过） |
| `journals/tool_stats.zig` | `.nulya/tool-usage.jsonl` `{v:1,at,session?,tool_id,version?,ok,duration_ms?}`：append（`at` 自己盖）+ 读回 + `aggregate`，**内核里没有读者** | 只是 evidence：晋升 = 人 / evolution session 写一条 pin（DESIGN §5.5、PLAN §3.7）。四列可选、`v` 仍是 1：老行原样读回、缺列 = null ≠ 0；`session` 让它 join outcome journal（durable session 是文件 stem，`ext run` 认 `NULYA_SESSION`），`duration_ms` 只由 loop 在 executor 两端用单调时钟量（不进 ledger、不出 `step()` 的返回值），`version` 是这次调用由哪个冻结实现服务的（session 从 `composition.extensions` 反查、`ext run` 用自己刚解析出的版本；builtin 与老行都是 null——**只写不读**，投影等第一个 consumer） |
| `journals/outcome.zig` | `.nulya/session-outcomes.jsonl`：`Verdict{success,partial,failure}` + `Source{human,agent}` + `by?` / `seq?` + `append`/`readAll`/`latestFor` | 没有行 = unknown ≠ failure；同 session 可多行、最后一条作数；三个可选列只在非默认时写（人评整场 = 老行逐字节相同），未知 `source` 是错不是人评；`latestFor` 只看整场行（`seq` 行是 turn 级证据） |
| `journals/trust.zig` | **user 层** `<NULYA_HOME \| ~/.nulya>/trusted-stores.jsonl` `{v:1,store:"<绝对 realpath>",at}`：`append` / `isTrusted` / `readAll`——workspace store 的 trust gate 的记录（DESIGN §9） | 第三条 journal，记的**不是证据而是一次授权**，所以在 user 层（project 层记不算数 = checkout 自己签名）；信任的是 **store 路径**不是内容 hash（hash 会被自演化每轮改掉）；重复行无害、幂等；home 不存在 = 还没记过（不是 fault），完整行 malformed = 显式错误（corrupt 不许悄悄变成 trusted 或 untrusted） |
| `cli/task.zig` | `nulya task run\|list\|status\|wait\|kill\|retarget` + 内部动词 `supervise`：后台任务的 supervisor 与它的读者面（DESIGN §6.1/§14）。**全部是壳层**，用现成的 `Tree` / `emit.headTail` / `ledger.depositEvent` / `journal.rfc3339Now` | supervisor 顺序承重（**先关掉 spawn 链漏进来的杂散 pipe 句柄**（`closeInheritedStrayPipes`，Windows：嵌套链上 `DetachedStdio` 护不到的那些，否则委派回执要等子 agent 跑完，DESIGN §8）→ 拿租约 → status `running` → 已有 kill 标记就不 spawn → `Tree` + 管道 drain 进 `output.log` → **deposit 后**才写 `done`）；`status.json` 是真相，`starting` / `lost` 只活在投影里（`lost` = 说 running 但 `.lock` 空闲；探针用 `openFile` 不用 `createFile`）；任务 id 是全名 `<sid>/t<N>`，投递名 `task-<owner>-t<N>.json`；`wait` 三个退出码 0/2/3，`--any` 只把**投递文件还在**的 done 算成 0（否则 driver 循环停不下来） |
| `cli.zig` + `cli/{common,session,session_list,step_stream,task,ext,ext_seed,config,skill,src,toolchain}.zig` | `cli.zig` 只有 `dispatch`（含 `help` / `--help` / `-h`；未知命令点名它自己并指向 `nulya help`）；一个动词族一个文件，共用件在 `cli/common.zig`（`printOut` / `printErrFmt`：stdout 只放数据与成功输出，拒绝与警告一律 stderr；**usage 按动词族拆成常量**——`help` 拼一屏，bare `nulya <family>` 印自己那块，一份文本两个用法）。`session.zig` 里只留驱动一场 session 的动词；`session_list.zig` 是 `.nulya/sessions/` 的只读投影，`step_stream.zig` 是 `--stream` 的行协议（TUI 逐字节解析，单独成文件）。`nulya ext …（含 --user；`ext trust` 无 `--user`——它只管本 workspace；`ext sync` / `ext prune` 是作用于整个 root 的两个动词，逐个 `<id>/` 做 build / 删非 current 版本，不改任何语义；`ext seed` 在 `cli/ext_seed.zig`——自带 draft 的写入与更新，判据是 `<root>/<id>/.seed` 记录；`ext inspect` 只答 store（`<id>` = 生效版本、`<id>@<v>`、`<path>` = draft）；`ext run` 缺省不套 timeout、`--timeout-ms` 才夹）` / `nulya session new [--parent] [--with] [--pin]\|append\|step\|events\|cancel\|outcome\|list` / `nulya config show` / `nulya src [path]` / `nulya skill list\|load` / `nulya toolchain zig` / `nulya ext api`  `ext/skill/toolchain/src/config` 经 `shell` 被模型调用；`session *` 是外部 driver 面（只有 `step` 写 session 文件）；都不是 LLM tool。`cli/ext.zig` 的 `printUntrustedStoreRefusal` 是 `cli/session.zig` 唯一跨文件借用的东西（refusal 要列 store 内容，与 `ext trust` 同一份 inventory 逻辑） |
| `source.zig` + `src_embed`（build.zig 生成） | `nulya src` 的数据：build.zig `@embedFile` 整个 `src/**`，`find`/`stripTests` 投影 | 默认剥 top-level `test` 块（靠 zig-fmt 第 0 列 `}` 不变量）、`--tests` 原样；测试留在文件里，剥的是投影不是存储 |
| `bundled.zig` + `ext_embed`（build.zig 生成） | `nulya ext seed` 的数据：`@embedFile` 整个 `extensions/**`（自带八个 draft 的源码），`ids`/`idOf` 投影 | 分发就是二进制本身；seed 只写源码进 store root，build/trust/activate 走原路（DESIGN §7.2/§7.8） |
| `launch.zig` | session 启动共享件（还有 `extensionRoots`（root 顺序）、`userHomeDir`、`sessionTasksDir`（`.nulya/scratch/<id>/tasks`，紧挨 `sessionScratchDir`）；时间戳搬去了 `journals/journal.zig`）：确定性 scripted provider（`NULYA_SCRIPTED_MODE`，六档 `finish|loop|truncate|handoff|batch|background`——`handoff` 演一次两阶段目标，让整条 /goal 回路离线可测）、`resolveDescriptor(profile, model_id?)`（创建时唯一一次 credential-aware 模型解析；codex 的 credential 是 auth.json 不是 env）+ `credentialAvailable`（同一判定，供 `config show` / `session new` 的提示）+ `buildFromDescriptor`（handle 只从 descriptor 建、无静默 fallback；`cache_key` = session id）、session id/path、**workspace store 的 trust gate**（`occupiedWorkspaceStore` = "持有"的唯一判据 + `ensureWorkspaceStoreTrusted` → `WorkspaceStoreUntrusted`，DESIGN §9） | CLI 与 demo 共用同一 durable 路径。门只在这一层：返回 error，由 `cli/session.zig` 翻成消息与 exit 1；内核（`composition.zig` / `session.zig`）不知道 trust 存在 |

## 构建与测试

```bash
zig build test      # 单元测试（每个模块同文件的 test 块，由 main.zig 聚合）
zig build e2e       # tests/e2e.zig 聚合 tests/e2e/*.zig：真实二进制的 extension 闭环 + 自造 + 按 pin 晋升
zig build e2e -Dtest-filter="follow-up"   # 只跑名字含该子串的测试（test / e2e / integration 都认，可重复）
zig build run       # nulya demo：固定 prompt 的一场 session（无 API key 时走 scripted provider）

# 唯一联网的测试（DESIGN §13.2）。不设变量就整体 skip，不会让没 key 的机器变红。
NULYA_INTEGRATION_PROFILE=deepseek-anthropic zig build integration
```

Zig 0.16（新 `std.Io` API）。发布版加 `-Dembed-toolchain -Dzig-archive=<path>` 内嵌工具链。

## 工作约定

- 代码注释英文，docs 中文。测试与模块同文件（`test "..."`）。
- **不加第二个 builtin tool**（`shell` 是唯一那个；`edit` 已搬进 `extensions/std`）；**不在 session 中途改 `tools[]`**；**不给 tool ledger**（需要对话的东西是 subagent，不是 tool）。
- 新增 kernel 概念前先问一句：**这是 substrate 还是 intelligence？** 是 intelligence 就放 kernel 之上。
- **内核只长 substrate，不长便利。** 往 `src/` 加东西前问：把它删掉，八条 physics 哪一条会失效？一条都不会 → 它不是内核。落点优先级：extension / skill（agent 自己造）> `cli.zig` / `launch.zig` 这类外壳 > kernel 模块。std 能做的不手写（`std.json` 类型化编解码、`union(enum)`）；一个字段只写不读、一个动词没有语义、一个决定在多层各做一遍、一个读者拿着写句柄——都是该删或该收的信号。
- **欠答案的机制长成 inbox 事件**（`task_finished` 是先例），不长成 driver 要认的新盘面文件：一个"结果稍后到"的回路如果靠某个目录里的文件形态来传递，每个 driver（TUI / `drivers/goal.sh` / `goal.ps1` / 下一个）都要重学一遍同一份 folklore，而跨平台就是两份实现。`extensions/handoff` 的文件形态是**历史特例**，不新增第二个。
- **第二个 consumer 出现之前不抽 abstraction。**
- 改 `§15.1 frozen core`（见 DESIGN.md）的语义要有明确理由并同步文档；往外挂能力优先于改 kernel。
- 引用设计条目用 `DESIGN §x` / `PLAN §x`，别引用 history/ 里的章节号。
