{
  "schema": "nulya.extension/v2",
  "id": "agent",
  "runtime": {
    "entry": "bin/agent"
  },
  "contributes": {
    "tools": [
      {
        "name": "agent",
        "surface": "auto",
        "description": "Delegate one self-contained piece of work to a sub-agent, or send another turn into a delegation that is already going. A sub-agent runs in a conversation of its own — its own context, its own tool face — and ONLY its report comes back to you, as a message a little later; nothing else from it enters this conversation and it cannot see yours. Give `name` to START one: say the whole task, because it sees nothing of this conversation and cannot ask you anything. Give `session` (the d-… id from a receipt or a report) INSTEAD to send it another turn — a correction, a request for specifics, a change of direction. That conversation still holds everything it found, so another turn costs one turn where starting over pays for the search again. Prefer it. It is accepted whether or not the sub-agent is working right now: by default it is taken at its next natural boundary, and `interrupt: true` stops what it is doing and has it take the message immediately. Use delegation for work that is separable and worth a fresh context (a broad search, a focused review); not for something you could finish in a step or two yourself, and never for the same thing twice. Calling an unknown name lists a short catalogue; for the full catalogue, use shell: `nulya ext run agent list`. After it returns, end your turn: the work has not happened yet.",
        "timeout_ms": 120000,
        "input": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Which agent definition to delegate to. Give this OR session, not both."
            },
            "session": {
              "type": "string",
              "description": "A delegation that is already going (`d-…`, as its receipt and its report name it), to send another turn into. Give this OR name, not both."
            },
            "task": {
              "type": "string",
              "description": "The whole task (or the whole follow-up) in its own words: what to do, what to look at, what counts as finished."
            },
            "model": {
              "type": "string",
              "description": "Which model runs this delegation. What the string means depends on the harness the definition names: for the default `runner: nulya` it is `<profile>` or `<profile>/<model-id>` (`nulya config show` lists them); for any other runner it is a model name in THAT harness's own vocabulary, passed through untouched. Only with name — a follow-up runs on what its conversation already froze. Omit unless you have a reason: without it the definition decides, and failing that a nulya sub-agent runs on the same model you do while an external one runs on whatever its harness is configured for. Give one when this piece of work is worth a different model than the conversation it came from."
            },
            "permissions": {
              "type": "string",
              "description": "How much this delegation may do: `readonly` (tools that only read — anything else is refused before it runs), `default` (ordinary work in this checkout: read, write, run things) or `unsafe` (everything its harness can do, with its own guard rails off). Only with name — a follow-up runs at the ceiling its delegation froze when it opened. Omit unless you have a reason: the definition already chose, and it usually chose right. `unsafe` is never inherited from anything — it happens because you write it here — so write it only when the task genuinely needs it and say why in the task itself; the person whose machine this is may be asked to approve this call and that sentence is what they read."
            },
            "interrupt": {
              "type": "boolean",
              "description": "Stop what the sub-agent is doing now and have it take this message immediately, instead of at its next natural boundary. Only with session — there is nothing to interrupt in a delegation that does not exist yet."
            }
          },
          "required": [
            "task"
          ]
        }
      },
      {
        "name": "render",
        "surface": "internal",
        "description": "Render an agent definition's body to a file a session can wear. For drivers: returns that file's path, for `session new --prompt`, plus the session arguments the definition asks for. Nothing is installed and nothing is versioned — the bytes are frozen into the session header when the session is created.",
        "input": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Which agent definition to render."
            }
          },
          "required": [
            "name"
          ]
        }
      },
      {
        "name": "list",
        "surface": "internal",
        "description": "Every agent definition this workspace, this machine and this package hold, in search order, with the ones an earlier layer shadows marked. For drivers: a picker's data. Never needed by a model — an unknown name already comes back with the names that exist.",
        "input": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "run",
        "surface": "internal",
        "description": "Drive one delegation until it has nothing left to answer, then print its report. For drivers: this is the command the background task started by `agent` runs; the report reaches the parent as that task's result. Everything about the delegation — which harness, which remote conversation, what it may do, how much of it is left — is read from its record, which is where those facts were frozen.",
        "input": {
          "type": "object",
          "properties": {
            "delegation": {
              "type": "string",
              "description": "The delegation to drive: whose runner lease this takes, whose interrupt marker it watches, and whose record says what it is."
            },
            "depth": {
              "type": "integer",
              "description": "How deep this delegation sits; the step it drives is told, so an indirect cycle of personas cannot recurse for ever. A fact about the chain rather than about the delegation, which is why it is an argument at all."
            }
          },
          "required": [
            "delegation"
          ]
        }
      }
    ]
  }
}
