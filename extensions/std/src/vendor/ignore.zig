//! `vendor/ignore` — STUB, owned by std-d (search) in docs/goals/std.md: a port of
//! zeegrep's `src/core/ignore.zig` (MIT) to Zig 0.16 std.Io.
const std = @import("std");
comptime {
    _ = std;
}
